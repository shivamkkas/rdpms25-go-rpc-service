// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// EdgeGateway is an object representing the database table.
type EdgeGateway struct {
	ID              int         `boil:"id" json:"id" toml:"id" yaml:"id"`
	Code            string      `boil:"code" json:"code" toml:"code" yaml:"code"`
	Alias           null.String `boil:"alias" json:"alias,omitempty" toml:"alias" yaml:"alias,omitempty"`
	OrganisationID  int         `boil:"organisation_id" json:"organisation_id" toml:"organisation_id" yaml:"organisation_id"`
	HexCode         null.String `boil:"hex_code" json:"hex_code,omitempty" toml:"hex_code" yaml:"hex_code,omitempty"`
	IsOnline        bool        `boil:"is_online" json:"is_online" toml:"is_online" yaml:"is_online"`
	LastConnectedOn null.Int64  `boil:"last_connected_on" json:"last_connected_on,omitempty" toml:"last_connected_on" yaml:"last_connected_on,omitempty"`
	BaseURL         null.String `boil:"base_url" json:"base_url,omitempty" toml:"base_url" yaml:"base_url,omitempty"`
	Enable          null.Bool   `boil:"enable" json:"enable,omitempty" toml:"enable" yaml:"enable,omitempty"`
	Meta            null.JSON   `boil:"meta" json:"meta,omitempty" toml:"meta" yaml:"meta,omitempty"`

	R *edgeGatewayR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L edgeGatewayL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var EdgeGatewayColumns = struct {
	ID              string
	Code            string
	Alias           string
	OrganisationID  string
	HexCode         string
	IsOnline        string
	LastConnectedOn string
	BaseURL         string
	Enable          string
	Meta            string
}{
	ID:              "id",
	Code:            "code",
	Alias:           "alias",
	OrganisationID:  "organisation_id",
	HexCode:         "hex_code",
	IsOnline:        "is_online",
	LastConnectedOn: "last_connected_on",
	BaseURL:         "base_url",
	Enable:          "enable",
	Meta:            "meta",
}

var EdgeGatewayTableColumns = struct {
	ID              string
	Code            string
	Alias           string
	OrganisationID  string
	HexCode         string
	IsOnline        string
	LastConnectedOn string
	BaseURL         string
	Enable          string
	Meta            string
}{
	ID:              "edge_gateway.id",
	Code:            "edge_gateway.code",
	Alias:           "edge_gateway.alias",
	OrganisationID:  "edge_gateway.organisation_id",
	HexCode:         "edge_gateway.hex_code",
	IsOnline:        "edge_gateway.is_online",
	LastConnectedOn: "edge_gateway.last_connected_on",
	BaseURL:         "edge_gateway.base_url",
	Enable:          "edge_gateway.enable",
	Meta:            "edge_gateway.meta",
}

// Generated where

type whereHelperbool struct{ field string }

func (w whereHelperbool) EQ(x bool) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperbool) NEQ(x bool) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperbool) LT(x bool) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperbool) LTE(x bool) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperbool) GT(x bool) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperbool) GTE(x bool) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }

type whereHelpernull_Int64 struct{ field string }

func (w whereHelpernull_Int64) EQ(x null.Int64) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, false, x)
}
func (w whereHelpernull_Int64) NEQ(x null.Int64) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, true, x)
}
func (w whereHelpernull_Int64) LT(x null.Int64) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpernull_Int64) LTE(x null.Int64) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpernull_Int64) GT(x null.Int64) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpernull_Int64) GTE(x null.Int64) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}
func (w whereHelpernull_Int64) IN(slice []int64) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelpernull_Int64) NIN(slice []int64) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

func (w whereHelpernull_Int64) IsNull() qm.QueryMod    { return qmhelper.WhereIsNull(w.field) }
func (w whereHelpernull_Int64) IsNotNull() qm.QueryMod { return qmhelper.WhereIsNotNull(w.field) }

var EdgeGatewayWhere = struct {
	ID              whereHelperint
	Code            whereHelperstring
	Alias           whereHelpernull_String
	OrganisationID  whereHelperint
	HexCode         whereHelpernull_String
	IsOnline        whereHelperbool
	LastConnectedOn whereHelpernull_Int64
	BaseURL         whereHelpernull_String
	Enable          whereHelpernull_Bool
	Meta            whereHelpernull_JSON
}{
	ID:              whereHelperint{field: "\"edge_gateway\".\"id\""},
	Code:            whereHelperstring{field: "\"edge_gateway\".\"code\""},
	Alias:           whereHelpernull_String{field: "\"edge_gateway\".\"alias\""},
	OrganisationID:  whereHelperint{field: "\"edge_gateway\".\"organisation_id\""},
	HexCode:         whereHelpernull_String{field: "\"edge_gateway\".\"hex_code\""},
	IsOnline:        whereHelperbool{field: "\"edge_gateway\".\"is_online\""},
	LastConnectedOn: whereHelpernull_Int64{field: "\"edge_gateway\".\"last_connected_on\""},
	BaseURL:         whereHelpernull_String{field: "\"edge_gateway\".\"base_url\""},
	Enable:          whereHelpernull_Bool{field: "\"edge_gateway\".\"enable\""},
	Meta:            whereHelpernull_JSON{field: "\"edge_gateway\".\"meta\""},
}

// EdgeGatewayRels is where relationship names are stored.
var EdgeGatewayRels = struct {
	Organisation string
}{
	Organisation: "Organisation",
}

// edgeGatewayR is where relationships are stored.
type edgeGatewayR struct {
	Organisation *Organisation `boil:"Organisation" json:"Organisation" toml:"Organisation" yaml:"Organisation"`
}

// NewStruct creates a new relationship struct
func (*edgeGatewayR) NewStruct() *edgeGatewayR {
	return &edgeGatewayR{}
}

func (o *EdgeGateway) GetOrganisation() *Organisation {
	if o == nil {
		return nil
	}

	return o.R.GetOrganisation()
}

func (r *edgeGatewayR) GetOrganisation() *Organisation {
	if r == nil {
		return nil
	}

	return r.Organisation
}

// edgeGatewayL is where Load methods for each relationship are stored.
type edgeGatewayL struct{}

var (
	edgeGatewayAllColumns            = []string{"id", "code", "alias", "organisation_id", "hex_code", "is_online", "last_connected_on", "base_url", "enable", "meta"}
	edgeGatewayColumnsWithoutDefault = []string{"code", "organisation_id"}
	edgeGatewayColumnsWithDefault    = []string{"id", "alias", "hex_code", "is_online", "last_connected_on", "base_url", "enable", "meta"}
	edgeGatewayPrimaryKeyColumns     = []string{"id"}
	edgeGatewayGeneratedColumns      = []string{}
)

type (
	// EdgeGatewaySlice is an alias for a slice of pointers to EdgeGateway.
	// This should almost always be used instead of []EdgeGateway.
	EdgeGatewaySlice []*EdgeGateway
	// EdgeGatewayHook is the signature for custom EdgeGateway hook methods
	EdgeGatewayHook func(context.Context, boil.ContextExecutor, *EdgeGateway) error

	edgeGatewayQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	edgeGatewayType                 = reflect.TypeOf(&EdgeGateway{})
	edgeGatewayMapping              = queries.MakeStructMapping(edgeGatewayType)
	edgeGatewayPrimaryKeyMapping, _ = queries.BindMapping(edgeGatewayType, edgeGatewayMapping, edgeGatewayPrimaryKeyColumns)
	edgeGatewayInsertCacheMut       sync.RWMutex
	edgeGatewayInsertCache          = make(map[string]insertCache)
	edgeGatewayUpdateCacheMut       sync.RWMutex
	edgeGatewayUpdateCache          = make(map[string]updateCache)
	edgeGatewayUpsertCacheMut       sync.RWMutex
	edgeGatewayUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var edgeGatewayAfterSelectMu sync.Mutex
var edgeGatewayAfterSelectHooks []EdgeGatewayHook

var edgeGatewayBeforeInsertMu sync.Mutex
var edgeGatewayBeforeInsertHooks []EdgeGatewayHook
var edgeGatewayAfterInsertMu sync.Mutex
var edgeGatewayAfterInsertHooks []EdgeGatewayHook

var edgeGatewayBeforeUpdateMu sync.Mutex
var edgeGatewayBeforeUpdateHooks []EdgeGatewayHook
var edgeGatewayAfterUpdateMu sync.Mutex
var edgeGatewayAfterUpdateHooks []EdgeGatewayHook

var edgeGatewayBeforeDeleteMu sync.Mutex
var edgeGatewayBeforeDeleteHooks []EdgeGatewayHook
var edgeGatewayAfterDeleteMu sync.Mutex
var edgeGatewayAfterDeleteHooks []EdgeGatewayHook

var edgeGatewayBeforeUpsertMu sync.Mutex
var edgeGatewayBeforeUpsertHooks []EdgeGatewayHook
var edgeGatewayAfterUpsertMu sync.Mutex
var edgeGatewayAfterUpsertHooks []EdgeGatewayHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *EdgeGateway) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range edgeGatewayAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *EdgeGateway) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range edgeGatewayBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *EdgeGateway) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range edgeGatewayAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *EdgeGateway) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range edgeGatewayBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *EdgeGateway) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range edgeGatewayAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *EdgeGateway) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range edgeGatewayBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *EdgeGateway) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range edgeGatewayAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *EdgeGateway) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range edgeGatewayBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *EdgeGateway) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range edgeGatewayAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddEdgeGatewayHook registers your hook function for all future operations.
func AddEdgeGatewayHook(hookPoint boil.HookPoint, edgeGatewayHook EdgeGatewayHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		edgeGatewayAfterSelectMu.Lock()
		edgeGatewayAfterSelectHooks = append(edgeGatewayAfterSelectHooks, edgeGatewayHook)
		edgeGatewayAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		edgeGatewayBeforeInsertMu.Lock()
		edgeGatewayBeforeInsertHooks = append(edgeGatewayBeforeInsertHooks, edgeGatewayHook)
		edgeGatewayBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		edgeGatewayAfterInsertMu.Lock()
		edgeGatewayAfterInsertHooks = append(edgeGatewayAfterInsertHooks, edgeGatewayHook)
		edgeGatewayAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		edgeGatewayBeforeUpdateMu.Lock()
		edgeGatewayBeforeUpdateHooks = append(edgeGatewayBeforeUpdateHooks, edgeGatewayHook)
		edgeGatewayBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		edgeGatewayAfterUpdateMu.Lock()
		edgeGatewayAfterUpdateHooks = append(edgeGatewayAfterUpdateHooks, edgeGatewayHook)
		edgeGatewayAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		edgeGatewayBeforeDeleteMu.Lock()
		edgeGatewayBeforeDeleteHooks = append(edgeGatewayBeforeDeleteHooks, edgeGatewayHook)
		edgeGatewayBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		edgeGatewayAfterDeleteMu.Lock()
		edgeGatewayAfterDeleteHooks = append(edgeGatewayAfterDeleteHooks, edgeGatewayHook)
		edgeGatewayAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		edgeGatewayBeforeUpsertMu.Lock()
		edgeGatewayBeforeUpsertHooks = append(edgeGatewayBeforeUpsertHooks, edgeGatewayHook)
		edgeGatewayBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		edgeGatewayAfterUpsertMu.Lock()
		edgeGatewayAfterUpsertHooks = append(edgeGatewayAfterUpsertHooks, edgeGatewayHook)
		edgeGatewayAfterUpsertMu.Unlock()
	}
}

// OneG returns a single edgeGateway record from the query using the global executor.
func (q edgeGatewayQuery) OneG(ctx context.Context) (*EdgeGateway, error) {
	return q.One(ctx, boil.GetContextDB())
}

// One returns a single edgeGateway record from the query.
func (q edgeGatewayQuery) One(ctx context.Context, exec boil.ContextExecutor) (*EdgeGateway, error) {
	o := &EdgeGateway{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for edge_gateway")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all EdgeGateway records from the query using the global executor.
func (q edgeGatewayQuery) AllG(ctx context.Context) (EdgeGatewaySlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// All returns all EdgeGateway records from the query.
func (q edgeGatewayQuery) All(ctx context.Context, exec boil.ContextExecutor) (EdgeGatewaySlice, error) {
	var o []*EdgeGateway

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to EdgeGateway slice")
	}

	if len(edgeGatewayAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all EdgeGateway records in the query using the global executor
func (q edgeGatewayQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// Count returns the count of all EdgeGateway records in the query.
func (q edgeGatewayQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count edge_gateway rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q edgeGatewayQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// Exists checks if the row exists in the table.
func (q edgeGatewayQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if edge_gateway exists")
	}

	return count > 0, nil
}

// Organisation pointed to by the foreign key.
func (o *EdgeGateway) Organisation(mods ...qm.QueryMod) organisationQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.OrganisationID),
	}

	queryMods = append(queryMods, mods...)

	return Organisations(queryMods...)
}

// LoadOrganisation allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (edgeGatewayL) LoadOrganisation(ctx context.Context, e boil.ContextExecutor, singular bool, maybeEdgeGateway interface{}, mods queries.Applicator) error {
	var slice []*EdgeGateway
	var object *EdgeGateway

	if singular {
		var ok bool
		object, ok = maybeEdgeGateway.(*EdgeGateway)
		if !ok {
			object = new(EdgeGateway)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeEdgeGateway)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeEdgeGateway))
			}
		}
	} else {
		s, ok := maybeEdgeGateway.(*[]*EdgeGateway)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeEdgeGateway)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeEdgeGateway))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &edgeGatewayR{}
		}
		args[object.OrganisationID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &edgeGatewayR{}
			}

			args[obj.OrganisationID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`organisation`),
		qm.WhereIn(`organisation.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Organisation")
	}

	var resultSlice []*Organisation
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Organisation")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for organisation")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for organisation")
	}

	if len(organisationAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Organisation = foreign
		if foreign.R == nil {
			foreign.R = &organisationR{}
		}
		foreign.R.EdgeGateways = append(foreign.R.EdgeGateways, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.OrganisationID == foreign.ID {
				local.R.Organisation = foreign
				if foreign.R == nil {
					foreign.R = &organisationR{}
				}
				foreign.R.EdgeGateways = append(foreign.R.EdgeGateways, local)
				break
			}
		}
	}

	return nil
}

// SetOrganisationG of the edgeGateway to the related item.
// Sets o.R.Organisation to related.
// Adds o to related.R.EdgeGateways.
// Uses the global database handle.
func (o *EdgeGateway) SetOrganisationG(ctx context.Context, insert bool, related *Organisation) error {
	return o.SetOrganisation(ctx, boil.GetContextDB(), insert, related)
}

// SetOrganisation of the edgeGateway to the related item.
// Sets o.R.Organisation to related.
// Adds o to related.R.EdgeGateways.
func (o *EdgeGateway) SetOrganisation(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Organisation) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"edge_gateway\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"organisation_id"}),
		strmangle.WhereClause("\"", "\"", 2, edgeGatewayPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.OrganisationID = related.ID
	if o.R == nil {
		o.R = &edgeGatewayR{
			Organisation: related,
		}
	} else {
		o.R.Organisation = related
	}

	if related.R == nil {
		related.R = &organisationR{
			EdgeGateways: EdgeGatewaySlice{o},
		}
	} else {
		related.R.EdgeGateways = append(related.R.EdgeGateways, o)
	}

	return nil
}

// EdgeGateways retrieves all the records using an executor.
func EdgeGateways(mods ...qm.QueryMod) edgeGatewayQuery {
	mods = append(mods, qm.From("\"edge_gateway\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"edge_gateway\".*"})
	}

	return edgeGatewayQuery{q}
}

// FindEdgeGatewayG retrieves a single record by ID.
func FindEdgeGatewayG(ctx context.Context, iD int, selectCols ...string) (*EdgeGateway, error) {
	return FindEdgeGateway(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindEdgeGateway retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindEdgeGateway(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*EdgeGateway, error) {
	edgeGatewayObj := &EdgeGateway{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"edge_gateway\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, edgeGatewayObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from edge_gateway")
	}

	if err = edgeGatewayObj.doAfterSelectHooks(ctx, exec); err != nil {
		return edgeGatewayObj, err
	}

	return edgeGatewayObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *EdgeGateway) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *EdgeGateway) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no edge_gateway provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(edgeGatewayColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	edgeGatewayInsertCacheMut.RLock()
	cache, cached := edgeGatewayInsertCache[key]
	edgeGatewayInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			edgeGatewayAllColumns,
			edgeGatewayColumnsWithDefault,
			edgeGatewayColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(edgeGatewayType, edgeGatewayMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(edgeGatewayType, edgeGatewayMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"edge_gateway\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"edge_gateway\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into edge_gateway")
	}

	if !cached {
		edgeGatewayInsertCacheMut.Lock()
		edgeGatewayInsertCache[key] = cache
		edgeGatewayInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single EdgeGateway record using the global executor.
// See Update for more documentation.
func (o *EdgeGateway) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// Update uses an executor to update the EdgeGateway.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *EdgeGateway) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	edgeGatewayUpdateCacheMut.RLock()
	cache, cached := edgeGatewayUpdateCache[key]
	edgeGatewayUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			edgeGatewayAllColumns,
			edgeGatewayPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update edge_gateway, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"edge_gateway\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, edgeGatewayPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(edgeGatewayType, edgeGatewayMapping, append(wl, edgeGatewayPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update edge_gateway row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for edge_gateway")
	}

	if !cached {
		edgeGatewayUpdateCacheMut.Lock()
		edgeGatewayUpdateCache[key] = cache
		edgeGatewayUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllG updates all rows with the specified column values.
func (q edgeGatewayQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q edgeGatewayQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for edge_gateway")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for edge_gateway")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o EdgeGatewaySlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o EdgeGatewaySlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), edgeGatewayPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"edge_gateway\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, edgeGatewayPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in edgeGateway slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all edgeGateway")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *EdgeGateway) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *EdgeGateway) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("models: no edge_gateway provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(edgeGatewayColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	edgeGatewayUpsertCacheMut.RLock()
	cache, cached := edgeGatewayUpsertCache[key]
	edgeGatewayUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			edgeGatewayAllColumns,
			edgeGatewayColumnsWithDefault,
			edgeGatewayColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			edgeGatewayAllColumns,
			edgeGatewayPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert edge_gateway, could not build update column list")
		}

		ret := strmangle.SetComplement(edgeGatewayAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(edgeGatewayPrimaryKeyColumns) == 0 {
				return errors.New("models: unable to upsert edge_gateway, could not build conflict column list")
			}

			conflict = make([]string, len(edgeGatewayPrimaryKeyColumns))
			copy(conflict, edgeGatewayPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"edge_gateway\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(edgeGatewayType, edgeGatewayMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(edgeGatewayType, edgeGatewayMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert edge_gateway")
	}

	if !cached {
		edgeGatewayUpsertCacheMut.Lock()
		edgeGatewayUpsertCache[key] = cache
		edgeGatewayUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single EdgeGateway record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *EdgeGateway) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// Delete deletes a single EdgeGateway record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *EdgeGateway) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no EdgeGateway provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), edgeGatewayPrimaryKeyMapping)
	sql := "DELETE FROM \"edge_gateway\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from edge_gateway")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for edge_gateway")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q edgeGatewayQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all matching rows.
func (q edgeGatewayQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no edgeGatewayQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from edge_gateway")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for edge_gateway")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o EdgeGatewaySlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o EdgeGatewaySlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(edgeGatewayBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), edgeGatewayPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"edge_gateway\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, edgeGatewayPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from edgeGateway slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for edge_gateway")
	}

	if len(edgeGatewayAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *EdgeGateway) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: no EdgeGateway provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *EdgeGateway) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindEdgeGateway(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *EdgeGatewaySlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: empty EdgeGatewaySlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *EdgeGatewaySlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := EdgeGatewaySlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), edgeGatewayPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"edge_gateway\".* FROM \"edge_gateway\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, edgeGatewayPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in EdgeGatewaySlice")
	}

	*o = slice

	return nil
}

// EdgeGatewayExistsG checks if the EdgeGateway row exists.
func EdgeGatewayExistsG(ctx context.Context, iD int) (bool, error) {
	return EdgeGatewayExists(ctx, boil.GetContextDB(), iD)
}

// EdgeGatewayExists checks if the EdgeGateway row exists.
func EdgeGatewayExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"edge_gateway\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if edge_gateway exists")
	}

	return exists, nil
}

// Exists checks if the EdgeGateway row exists.
func (o *EdgeGateway) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return EdgeGatewayExists(ctx, exec, o.ID)
}
